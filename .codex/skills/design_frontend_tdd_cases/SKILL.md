---
name: design_frontend_tdd_cases
description: "인터페이스 계약을 바탕으로 UI 렌더링과 사용자 이벤트에 대한 TDD 기반 블랙박스 테스트 코드를 작성합니다. 테스트 도구는 아키텍처가 확정한 스택을 따릅니다. 구현 코드를 직접 보거나 서버 연동 테스트를 짜는 작업에는 절대 이 스킬을 트리거하지 마십시오."
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---

# Skill: Frontend UI TDD Case Design

## Workflow (작업 순서)

1. **테스트 스택 확인 (Stack Binding)**
   - 시스템 프롬프트 최상단에 **이미 주입된** `<design_spec>` 블록의 「기술 스택」·「소유권」·「표준 명령어」 섹션에서 프론트엔드 테스트 프레임워크, 렌더링 테스트 라이브러리, 사용자 이벤트 시뮬레이션 도구, 네트워크 모킹 도구, 테스트 경로, 실행 명령을 확인한다.
   - 🚨 **주의:** `design.md`를 `Read` 등 어떤 도구로도 읽지 않는다. 전문이 이미 주입되어 있으며 중복 조회는 규약 위반이다.
   - 🚨 **주의:** 테스트 도구를 스스로 가정하지 않는다. 명시가 없으면 작성을 멈추고 `[SPEC GAP]`을 붙여 호출자에게 질의한다.

2. **계약 분석 및 클린 룸 준수 (Contract Review)**
   - `03_contracts/`의 UI 입력 규격 및 상태 계약을 파악한다. 절대 실제 컴포넌트 구현을 열어보지 않는다.

3. **사용자 관점 시나리오 기획 (User-Centric Planning)**
   - 확정된 테스트 도구의 문법으로 시나리오를 짠다.
   - 내부 상태 값이 아니라, 실제 화면에 보이는 **렌더링 결과(역할, 텍스트, 라벨, 접근성 속성)**를 기준으로 검증문을 작성한다.

4. **이벤트 및 에러 모킹 (Event & Error Mocking)**
   - 확정된 이벤트 시뮬레이션 도구로 실제 사용자의 클릭·입력·이동 흐름을 재현한다.
   - `<design_spec>`이 정한 네트워크 모킹 계층으로 서버 오류 응답과 실시간 연결 단절 상황을 모킹하고, 이때 **폴백 UI와 오류 격리**가 정상 노출되는지 검증한다.

5. **산출물 적재 및 알림 (Notify)**
   - 코드를 소유권 표의 프론트엔드 테스트 경로에 스택 관용 네이밍으로 저장한다.
   - `Bash`로 표준 테스트 명령을 실행해 구동 가능성과 **의도된 실패(Red)**를 확인한 뒤, "UI 실패하는(Red) 테스트 준비 완료" 사실을 최종 보고에 담아 반환한다. 오케스트레이터가 이를 읽어 `frontend-developer`를 스폰한다.

## Why (왜 이렇게 하는가?)

- **UX 중심의 품질 보장:** 프론트엔드는 함수가 도는지가 중요한 게 아니라 '사용자의 눈에 화면이 제대로 보이고 눌리는가'가 핵심이다. 철저하게 렌더링 결과와 사용자 이벤트 관점에서만 테스트를 강제하기 위함이다.
- **스택 독립성:** 렌더링 테스트 도구는 프레임워크마다 다르지만, "구현을 보지 않고 사용자에게 보이는 결과만 검증한다"는 원칙은 동일하다. 도구 선택을 아키텍처에 위임해 원칙만 재사용하기 위함이다.
